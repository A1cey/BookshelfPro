spring:
  application:
    name: bookshelf-pro
  datasource:
    url: jdbc:postgresql://localhost:5432/bookshelf-pro
    username: admin
    password: admin
  docker:
    compose:
      lifecycle-management: start_only
